---
tags: [moc, home]
---

# 🧠 rombik — мозок проєкту

Це «другий мозок» проєкту **rombik** — інструмента, що перетворює код на
блок-схему алгоритму за ДСТУ 19.701-90. Тут пояснено **все**: від задуму до
кожного рядка реалізації.

> [!tip] Як читати
> Це Obsidian-vault. Посилання в подвійних дужках (`[[…]]`) ведуть між нотатками.
> Почни з огляду, далі — архітектура, потім — конкретні компоненти.

---

## 🗺️ Карта

### 00 — Огляд

- [[Задум-і-мотивація]] — навіщо це взагалі і яку біль розв'язує
- [[ДСТУ-19.701-90]] — стандарт блок-схем, під який усе зроблено
- [[Глосарій]] — терміни проєкту в одному місці

### 01 — Архітектура

- [[Архітектура-огляд]] — пакети, межі, потік даних
- [[Конвеєр-обробки]] — код → AST → IR → геометрія → SVG/Typst/PNG/PDF
- [[Чому-AST-JSON-як-контракт]] — ключове архітектурне рішення
- [[Розділення-відповідальностей]] — ядро vs адаптери, чому однаково в CLI і WASM

### 02 — Компоненти (пакети)

- [[Парсер-Python]] — `pkg/parser/python` (parser.py + python.go)
- [[astjson-конвертер]] — `pkg/parser/astjson`
- [[IR-проміжне-представлення]] — `pkg/ir`
- [[Layout-рушій-розкладки]] — `pkg/layout` ⭐ серце проєкту
- [[Diagram-модель-геометрії]] — `pkg/diagram` (+ підпис «Рисунок N»)
- [[SVG-рендерер]] — `pkg/render/svg`
- [[Typst-рендер]] — `pkg/render/typst` (CeTZ → PDF через Typst)
- [[Растровий-рендер-PNG-PDF]] — `pkg/render/raster` (нативні PNG/PDF)
- [[Route-маршрутизатор-ребер]] — `pkg/route`
- [[Публічний-API-rombik]] — `pkg/rombik` (фасад для бібліотечного вжитку)

### 03 — Алгоритми розкладки

- [[Розкладка-рекурсією-size-і-place]] — як дерево стає координатами
- [[Розкладка-if-guard-і-симетрія]]
- [[Розкладка-циклів]] — for, while, do-while, нескінченний; дуги; continue; for/else
- [[Зведення-виходів-у-Кінець]] — single-end vs per-exit, break
- [[A-зірка-маршрутизація]] — сітка Ганана зі штрафом за повороти

### 04 — Веб

- [[Браузерний-рушій]] — Tree-sitter + два WASM, нуль серверного коду
- [[WASM-міст]] — `cmd/wasm` (легкий) + `cmd/wasmraster` (важкий)
- [[Фронтенд-SvelteKit]] — CodeMirror-редактор, темна тема, експорт SVG/PNG/PDF/Typst

### 05 — Використання

- [[CLI-довідник]] — прапорці, формати (svg/png/pdf/typ/json/excalidraw), приклади
- [[Опції-рендера]] — кожна галочка інтерфейсу + розбиття схем + підпис
- [[Підтримувані-конструкції-Python]] — що розпізнається, що ігнорується (тепер є і підтримка C++)

### 06 — Розробка

- [[Збірка-і-запуск]] — Go, два WASM, фронтенд, релізи
- [[Як-додати-нову-мову]] — C++/JS через той самий AST-JSON
- [[Ідеї-та-межі]] — що свідомо не зроблено і чому
- [[Структура-репозиторію]] — повне дерево + карта файл→нотатка

---

## 📌 За 30 секунд

rombik — це **компілятор зі структурованого коду в блок-схему**. Архітектура —
класичний конвеєр компілятора:

1. **Фронтенд парсера** (Tree-sitter у вебі, або Python `ast` у CLI) видає **мова-агностик AST-JSON**.
2. **astjson** зводить його в **[[IR-проміжне-представлення|IR]]** — чисте дерево
   керування без жодної геометрії.
3. **[[Layout-рушій-розкладки|layout]]** рекурсивно розкладає IR у **[[Diagram-модель-геометрії|Diagram]]**
   (фігури з (x,y) + ребра-ламані).
4. **Бекенди рендера** друкують результат: [[SVG-рендерер|SVG]], [[Typst-рендер|Typst]],
   [[Растровий-рендер-PNG-PDF|PNG/PDF]], Excalidraw — усе **нативно в Go, без зовнішніх бінарників** (або на клієнті).

Ядро не знає ні про Python, ні про ОС, ні про формат виводу — тому однаково крутиться
в CLI і в браузері (два WASM-модулі). Для бібліотечного вжитку — фасад
[[Публічний-API-rombik|rombik.FromPython/FromAST]].
