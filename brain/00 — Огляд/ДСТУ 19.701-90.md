---
tags: [overview, standard, dstu]
---

# ДСТУ 19.701-90

ДСТУ ISO 19.701-90 (історично ГОСТ 19.701-90, «Схеми алгоритмів, програм, даних і
систем») — стандарт, що визначає **умовні графічні позначення** блок-схем. rombik
малює саме ці фігури. Нижче — як кожна з них представлена в проєкті.

## Фігури → типи в коді

Тип фігури задається `diagram.Kind` ([[Diagram — модель геометрії]]). Машинні назви
(колонка JSON) використовує фронтенд.

| ДСТУ-фігура              | `diagram.Kind` | JSON-назва    | Геометрія SVG                        | Звідки в IR        |
|--------------------------|----------------|---------------|--------------------------------------|--------------------|
| Термінатор (початок/кінець) | `Terminator` | `terminator`  | `<rect>` з `rx = h/2` (овал-стадіон) | обгортка `Build`   |
| Процес (дія)             | `Process`      | `process`     | `<rect>` (гострі кути)                | `ir.Process`       |
| Розв'язок (умова)        | `Decision`     | `decision`    | `<polygon>` ромб (4 точки)           | `ir.If`, `While`, `DoWhile` |
| Ввід/вивід               | `InOut`        | `io`          | `<polygon>` паралелограм (нахил 0.4h) | `ir.IO`           |
| Початок циклу            | `Hexagon`      | `loop`        | `<polygon>` шестикутник (скоси 0.5h) | `ir.For`           |
| Підготовлений процес (підпрограма) | `Predef` | `subprogram` | `<rect>` + дві вертикальні риски всередині | `ir.Call`     |

Точні координати кожної фігури рахує [[SVG-рендерер]] (`renderShape`).

## Конвенції «курсової схеми»

Стандарт лишає свободу; rombik дотримується усталених у методичках конвенцій:

- **Параметри функції — вхідний паралелограм.** `def f(a, b)` додає першим вузлом
  «Ввід a, b». Робить це парсер у `funcblock` ([[Парсер Python]]).
- **`for i in range(...)` — шестикутник** із підписом `i = початок, кінець, крок`,
  де кінець = `stop - 1` (бо `range` напіввідкритий). Логіка — `forspec` у parser.py.
- **Виклик власної функції — символ підпрограми** (`Predef`), а не процес. Лише для
  функцій, визначених у тому ж файлі. Можна вимкнути опцією `CallAsProcess`
  ([[Опції рендера]]).
- **`return` / `raise` / `exit` — завершення гілки**, що веде у «Кінець»
  (`ir.Terminal`). За замовчуванням — окремий «Кінець» на кожен вихід; опція
  `SingleEnd` зводить усе в один. → [[Зведення виходів у Кінець]].
- **Гілки розв'язку підписані «Так»/«Ні»** (налаштовується).

## Чому власні SVG-примітиви, а не бібліотека

[[SVG-рендерер]] малює фігури вручну (`<polygon>`/`<rect>` з порахованими точками).
Це дає **піксельний контроль** над геометрією — критично, щоб шестикутник і
паралелограм виглядали точно за ДСТУ, а не «приблизно як у Graphviz».

## Пов'язане

- [[Diagram — модель геометрії]]
- [[SVG-рендерер]]
- [[Опції рендера]]
- [[Глосарій]]
